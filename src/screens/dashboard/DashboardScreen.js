import React from 'react';import {View,Text} from 'react-native';import AppCard from '../../components/AppCard';const cards=[['Reservations','Réservations'],['Courses','Courses'],['Reparations','Réparations'],['Utilisateurs','Utilisateurs']];export default function DashboardScreen({navigation}){return <View style={{padding:16}}><Text>Dashboard</Text>{cards.map(([route,title])=><AppCard key={route} title={title} subtitle={`Voir la liste ${title}`} onPress={()=>navigation.navigate(route)}/>)}</View>}
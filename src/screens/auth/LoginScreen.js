import React,{useState} from 'react';import {View,Text} from 'react-native';import AppInput from '../../components/AppInput';import AppButton from '../../components/AppButton';import ErrorMessage from '../../components/ErrorMessage';import { useAuth } from '../../context/AuthContext';
export default function LoginScreen({navigation}){const {login}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');const [loading,setLoading]=useState(false);const onSubmit=async()=>{try{setLoading(true);setErr('');await login({email,password});}catch(e){setErr(e.message)}finally{setLoading(false)}};return <View style={{padding:16}}><Text>Connexion</Text><ErrorMessage message={err}/><AppInput label='Email' value={email} onChangeText={setEmail}/><AppInput label='Mot de passe' value={password} onChangeText={setPassword} secureTextEntry/><AppButton title='Se connecter' onPress={onSubmit} loading={loading}/><AppButton title='Créer un compte' onPress={()=>navigation.navigate('Register')} variant='secondary'/></View>;}